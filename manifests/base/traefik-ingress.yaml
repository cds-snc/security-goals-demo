apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: traefik-ingress
spec:
  rules:
    - http:
        paths:
          - path: /security-goals
            backend:
              serviceName: api
              servicePort: 3000
